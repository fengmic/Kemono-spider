## 主要更新

### 原生 WPF 工作台

- 使用原生 WPF 和 MVVM 重构桌面界面
- 保留作者合集、单作品、断点续传、设置、缺失补足与损坏修复等功能
- 深色工作台风格，支持非最大化窗口滚动布局
- 记忆窗口位置、大小、最大化状态和常用页面配置
- 提供正式应用图标和 Windows x64 自包含单文件发布

### 作者任务队列

- 当前作者合集下载运行时，可以继续添加新的作者任务
- 每个任务独立记录服务器、平台、作者 ID、保存路径和下载参数
- 当前任务完成后自动执行下一个队列任务
- 队列项显示作者名称、平台、作者 ID、服务器、进度和当前文件
- 支持移除等待中的任务、清理已结束任务
- 手动停止后队列暂停，可使用“继续队列”恢复

### 自适应下载与断点续传

- 附件下载采用响应头超时、连续无数据超时、低速窗口和动态总时长上限
- 默认最大尝试次数调整为 3 次，仍支持设置 1–20 次
- 大文件持续传输时不再受旧固定总时长限制
- 使用 `.part` 和 `.part.meta.json` 保存断点、长度、ETag 和 Last-Modified
- 支持重试和软件重启后的 HTTP Range 续传
- 自动处理服务器忽略 Range、验证器变化、416 和响应长度不完整等情况
- 404、410、401、403、HTML/JSON 错误页和零长度响应快速失败
- 连续两次无新增数据时提前终止，减少对损坏资源的无谓等待

### 服务器与作者资料兼容

- 内置 `kemono.cr`、`kemono.su` 和 `pawchive.pw`
- 支持添加自定义服务器域名
- 统一 Cookie、代理、Referer、Accept-Language 和自动解压请求行为
- 支持 HTTP、HTTPS、SOCKS5 代理及代理认证
- 作者名称兼容 `public_id` 和 `name` 字段
- Pawchive 等站点返回 `public_id: null` 时使用 `name`
- 已生成的 `user_<作者ID>` 目录在目标名称可用时自动迁移

### 缺失补足与批量修复

- 缺失扫描结果按作品分组，可展开查看具体顺序文件和原始附件名
- 支持使用缩略图补足无法获取的原图附件
- 支持选择作者合集的上一级目录，批量扫描多个作者合集
- 批量补足或修复按作者顺序执行，每个作者内部使用设定的并发数
- 补足完成后列表只保留仍然缺失的文件

### 配置与进度

- 设置保存到 `%AppData%/KemonoDownloader/settings.json`
- 代理密码使用 Windows DPAPI 保护
- 进度文件使用 `schema_version: 2`
- 旧 Electron 进度格式不会被静默覆盖，会显示不支持提示
- 旧设置中的 5 次重试和视频 1200 秒默认值会自动迁移为 3 次和 120 秒
- 断点续传始终以用户选择的作者目录为准，不受其他页面保存路径影响

## 兼容性说明

- 目标平台：Windows 10 或更高版本，Windows x64
- 发布包为自包含单文件，不需要预安装 .NET Desktop Runtime
- 不再提供 Electron、Node.js、macOS 或 Linux 并行构建入口
- 不承诺读取旧 Electron 生成的进度文件；请使用新版重新建立任务

## 免责声明

本项目仅供学习和研究使用。使用者应遵守目标网站的服务条款、当地法律及内容版权要求，并自行承担使用风险。
